// lib/aiProviderCore.js
import{getModelConfig}from'./modelRegistry';import{logAIUsage}from'./aiObserver';import sharp from'sharp';import crypto from'crypto';
const GROQ_KEY=process.env.GROQ_API_KEY,GEMINI_KEY=process.env.GEMINI_API_KEY,GROQ_URL='https://api.groq.com/openai/v1/chat/completions',GEMINI_MODEL=process.env.GEMINI_SCAN_MODEL||'gemini-2.5-flash-lite',PRIMARY_KEY='groq-qwen3.8-27b',FALLBACK_KEY='groq-qwen3.6-27b',PRIMARY=getModelConfig(PRIMARY_KEY),FALLBACK=getModelConfig(FALLBACK_KEY);export const SCAN_PIPELINE_VERSION='v65-hybrid-multiregion-crosscheck';
const MAX=50,DEADLINE=120000,REGION_TIMEOUT=55000;
const PROMPT=`Read this handwritten register as a physical document. Accuracy of characters and phone-to-person ownership is more important than completing every row.
RULES:
1. Read every visible person from top to bottom. Preserve the written name literally; never autocorrect, complete, shorten or replace an unfamiliar Nigerian/African/Christian name.
2. Preserve phone digits literally. Never repair a digit because a Nigerian number looks more plausible. If a digit is unclear, keep your best visible reading and flag digit_uncertain.
3. A person can have two phone numbers. A blank-name line directly below a named row can be that person's continuation only when the physical position supports it.
4. Never borrow a phone from a nearby row. Ownership follows physical placement, not semantic plausibility.
5. Ignore headers, totals, dates and unrelated notes.
6. The supplied image is a crop of a larger page. Return only rows visible in this crop and use y as the vertical center of each row from 0 to 1000 within this crop.
7. Include observable evidence only, such as same row, continuation directly below, aligned phone column, or digit visually unclear. Do not expose chain-of-thought.
Return ONLY JSON with this shape: {"people":[{"r":1,"y":500,"n":"visible name","p":["visible phone"],"f":[],"e":"same physical row"}]}. Flags may only be digit_uncertain, continuation_uncertain, row_ownership_uncertain, name_uncertain. Maximum ${MAX} rows.`;
const SCHEMA={type:'json_schema',json_schema:{name:'nyeocare_register_region',strict:true,schema:{type:'object',additionalProperties:false,properties:{people:{type:'array',maxItems:MAX,items:{type:'object',additionalProperties:false,properties:{r:{type:'integer',minimum:1,maximum:MAX},y:{type:'integer',minimum:0,maximum:1000},n:{type:'string'},p:{type:'array',maxItems:2,items:{type:'string'}},f:{type:'array',items:{type:'string',enum:['digit_uncertain','continuation_uncertain','row_ownership_uncertain','name_uncertain']}},e:{type:'string'}},required:['r','y','n','p','f','e']}}},required:['people']}}};
const clean=v=>String(v??'').replace(/\s+/g,' ').trim(),nameKey=v=>clean(v).toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z\s'\-]/g,' ').replace(/\s+/g,' ').trim(),digits=v=>String(v??'').replace(/\D/g,'');
function normalizeImage(input){let s=String(input||'').trim(),mime=null,m=s.match(/^data:([^;,]+);base64,(.*)$/is);if(m){mime=String(m[1]).toLowerCase();s=m[2]}s=s.replace(/\s/g,'').replace(/-/g,'+').replace(/_/g,'/');if(!s||s.length%4===1||!/^[A-Za-z0-9+/]*={0,2}$/.test(s))throw Object.assign(new Error('The scan image encoding is invalid.'),{code:'INVALID_IMAGE'});const b=Buffer.from(s,'base64'),canonical=b.toString('base64').replace(/=+$/,''),provided=s.replace(/=+$/,'');if(!b.length||canonical!==provided||b.length>4000000)throw Object.assign(new Error('The scan image could not be safely decoded.'),{code:'INVALID_IMAGE'});if(b[0]===255&&b[1]===216&&b[2]===255)mime='image/jpeg';else if(b[0]===137&&b[1]===80&&b[2]===78&&b[3]===71)mime='image/png';else if(b.slice(0,4).toString('ascii')==='RIFF'&&b.slice(8,12).toString('ascii')==='WEBP')mime='image/webp';else throw Object.assign(new Error('The scan image format is not supported.'),{code:'INVALID_IMAGE'});return{buffer:b,mime,base64:b.toString('base64')}}
async function makeRegions(input){const src=sharp(input).rotate(),m=await src.metadata();if(!m.width||!m.height)throw Object.assign(new Error('The scan image has no readable dimensions.'),{code:'INVALID_IMAGE'});const width=Math.min(2600,m.width),base=await src.resize({width,withoutEnlargement:false}).normalize().sharpen({sigma:1.1}).jpeg({quality:92,mozjpeg:true}).toBuffer({resolveWithObject:true}),W=base.info.width,H=base.info.height;if(H<900){return[{id:1,y0:0,y1:H,width:W,height:H,base64:base.data.toString('base64'),mime:'image/jpeg'}]}const overlap=Math.round(H*.30),height=Math.round(H*.62),topHeight=Math.min(H,height),top={id:1,y0:0,y1:topHeight,width:W,height:topHeight},bottomY=Math.max(0,H-height),bottom={id:2,y0:bottomY,y1:H,width:W,height:H-bottomY};const regions=[top,bottom];return Promise.all(regions.map(async r=>({...r,base64:(await sharp(base.data).extract({left:0,top:r.y0,width:W,height:r.height}).jpeg({quality:92,mozjpeg:true}).toBuffer()).toString('base64'),mime:'image/jpeg'})))}
function parse(x){if(typeof x!=='string')return null;const s=x.replace(/<think>[\s\S]*?<\/think>/gi,'').replace(/```json|```/gi,'').trim();try{const v=JSON.parse(s);return v&&Array.isArray(v.people)?v:null}catch{return null}}
function durationSeconds(v){if(v==null)return 0;const s=String(v).trim().toLowerCase();if(/^\d+(?:\.\d+)?$/.test(s))return Number(s);let n=0,m,re=/(\d+(?:\.\d+)?)\s*(ms|s|m|h)/g;while((m=re.exec(s)))n+=Number(m[1])*({ms:.001,s:1,m:60,h:3600}[m[2]]);return n}
function rateLimitState(h){return{remainingTokens:Number(h.get('x-ratelimit-remaining-tokens')),remainingRequests:Number(h.get('x-ratelimit-remaining-requests')),resetTokens:durationSeconds(h.get('x-ratelimit-reset-tokens')),resetRequests:durationSeconds(h.get('x-ratelimit-reset-requests')),retryAfter:durationSeconds(h.get('retry-after'))}}
async function groqRegion(region,opt={},attempt=1){if(!GROQ_KEY)throw Object.assign(new Error('GROQ_API_KEY is missing'),{code:'AI_NOT_CONFIGURED'});const model=PRIMARY,ac=new AbortController(),timer=setTimeout(()=>ac.abort(),REGION_TIMEOUT),rid=crypto.randomUUID(),started=Date.now();try{const body={model:model.model,messages:[{role:'user',content:[{type:'text',text:`${PROMPT}\nThis is region ${region.id} of the page. Region bounds in the original page are y=${region.y0}..${region.y1}.`},{type:'image_url',image_url:{url:`data:${region.mime};base64,${region.base64}`}}]}],temperature:0,top_p:.9,reasoning_effort:'high',reasoning_format:'hidden',max_completion_tokens:Math.min(5200,model.max_completion_tokens),response_format:SCHEMA},r=await fetch(GROQ_URL,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${GROQ_KEY}`},body:JSON.stringify(body),signal:ac.signal}),d=await r.json().catch(()=>({})),finish=d?.choices?.[0]?.finish_reason||null,raw=d?.choices?.[0]?.message?.content||'',u=d?.usage||{},rl=rateLimitState(r.headers);await logAIUsage({organization_id:opt.organization_id,job_id:opt.job_id,request_id:rid,model_key:PRIMARY_KEY,provider:'groq',model:model.model,purpose:`scan_region_${region.id}`,prompt_version:SCAN_PIPELINE_VERSION,attempt,latency_ms:Date.now()-started,input_tokens:Number(u.prompt_tokens||0),output_tokens:Number(u.completion_tokens||0),reasoning_tokens:Number(u.completion_tokens_details?.reasoning_tokens||0),total_tokens:Number(u.total_tokens||0),http_status:r.status,success:r.ok,finish_reason:finish,rate_limit_remaining_tokens:r.headers.get('x-ratelimit-remaining-tokens'),rate_limit_remaining_requests:r.headers.get('x-ratelimit-remaining-requests'),rate_limit_reset_tokens:r.headers.get('x-ratelimit-reset-tokens'),rate_limit_reset_requests:r.headers.get('x-ratelimit-reset-requests'),retry_after:r.headers.get('retry-after')}).catch(()=>{});if(!r.ok){const em=d?.error?.message||`Groq request failed (${r.status})`;if(r.status===429){const wait=Math.max(rl.retryAfter,rl.resetTokens,rl.resetRequests);throw Object.assign(new Error(em),{code:'AI_RATE_LIMIT',status:r.status,retryAfter:rl.retryAfter||null,resetAfterSeconds:wait||null,cooldownSeconds:wait||null,requestId:rid,provider:model.model,rateLimit:rl})}throw Object.assign(new Error(em),{code:r.status===404?'AI_MODEL_UNAVAILABLE':'AI_PROVIDER_ERROR',status:r.status,requestId:rid,provider:model.model,rateLimit:rl})}if(finish==='length')throw Object.assign(new Error('The model stopped before completing this register region.'),{code:'AI_OUTPUT_TRUNCATED',requestId:rid,provider:model.model});const parsed=parse(raw);if(!parsed)throw Object.assign(new Error('The model returned invalid register JSON.'),{code:'AI_INVALID_RESPONSE',requestId:rid,provider:model.model});return{region:region.id,people:parsed.people,model:model.model,requestId:rid,rateLimit:rl}}catch(e){if(e?.name==='AbortError')throw Object.assign(new Error(`Qwen vision timed out on register region ${region.id}.`),{code:'AI_TIMEOUT',requestId:rid,provider:model.model});throw e}finally{clearTimeout(timer)}}
function globalY(local,region){return region.y0+(Math.max(0,Math.min(1000,Number(local)||0))/1000)*(region.y1-region.y0)}
function phoneSet(x){return(Array.isArray(x?.p)?x.p:[]).map(clean).filter(Boolean)}
function similarity(a,b){const x=nameKey(a),y=nameKey(b);if(!x||!y)return 0;if(x===y)return 1;const ax=x.split(/\s+/),ay=y.split(/\s+/);let hit=0;for(const t of ax){let best=0;for(const u of ay){const n=Math.max(t.length,u.length),d=Math.abs(t.length-u.length);let same=0;for(let i=0;i<Math.min(t.length,u.length);i++)if(t[i]===u[i])same++;best=Math.max(best,(same-d)/Math.max(1,n))}hit+=Math.max(0,best)}return hit/Math.max(1,ax.length)}
function mergeRegions(parts){const observations=[];for(const part of parts){const region=part.regionObj;for(const p of part.people||[]){const phones=phoneSet(p);if(!clean(p.n)&&!phones.length)continue;observations.push({y:globalY(p.y,region),name:clean(p.n),phones,flags:Array.isArray(p.f)?p.f:[],evidence:clean(p.e),region:part.region})}}observations.sort((a,b)=>a.y-b.y);const groups=[];for(const o of observations){let best=null,score=-1;for(const g of groups){const dy=Math.abs(g.y-o.y),nameScore=o.name&&g.name?similarity(o.name,g.name):0,phoneOverlap=o.phones.some(p=>g.phones.some(q=>digits(p)&&digits(p)===digits(q)));const s=(dy<=Math.max(45,g.pageHeight*.022)?1:0)+nameScore+(phoneOverlap?1.5:0);if(s>score&&dy<=Math.max(55,g.pageHeight*.032)){score=s;best=g}}if(!best)groups.push({y:o.y,name:o.name,phones:[...o.phones],flags:[...o.flags],evidence:[o.evidence],regions:[o.region],pageHeight:part.pageHeight});else{if(o.name&&(!best.name||o.name.length>best.name.length))best.name=o.name;for(const p of o.phones)if(!best.phones.some(q=>digits(q)===digits(p)))best.phones.push(p);best.flags.push(...o.flags);best.evidence.push(o.evidence);best.regions.push(o.region);best.y=(best.y+o.y)/2}}return groups.sort((a,b)=>a.y-b.y).slice(0,MAX).map((g,i)=>{const uniqueFlags=[...new Set(g.flags)];const regionDisagreement=new Set(g.regions).size>1&&g.name&&g.phones.length===0;return{r:i+1,y:Math.round((g.y/(g.pageHeight||1))*1000),n:g.name,p:g.phones.slice(0,2),f:[...uniqueFlags,...(regionDisagreement?['row_ownership_uncertain']:[])],e:[...new Set(g.evidence.filter(Boolean))].slice(0,3).join('; ')}})}
async function geminiVerify(inputBase64,candidates,opt){if(!GEMINI_KEY)return null;const prompt=`Verify the handwritten register against these machine candidates. Do not invent corrections. Return JSON with the same physical rows. If a name or phone differs visually, preserve the candidate only when the image supports it; otherwise return your best literal transcription and flag the row. Focus especially on phone digits and continuation ownership. Candidate JSON:\n${JSON.stringify({people:candidates})}`;const schema={type:'object',properties:{people:{type:'array',items:{type:'object',properties:{r:{type:'integer'},n:{type:'string'},p:{type:'array',items:{type:'string'},maxItems:2},f:{type:'array',items:{type:'string'}},e:{type:'string'}},required:['r','n','p','f','e'],additionalProperties:false},maxItems:MAX}},required:['people'],additionalProperties:false};const ac=new AbortController(),timer=setTimeout(()=>ac.abort(),50000);try{const url=`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(GEMINI_MODEL)}:generateContent?key=${encodeURIComponent(GEMINI_KEY)}`,body={contents:[{role:'user',parts:[{text:prompt},{inlineData:{mimeType:'image/jpeg',data:inputBase64}}]}],generationConfig:{temperature:0,responseMimeType:'application/json',responseSchema:schema,maxOutputTokens:6000}},r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body),signal:ac.signal}),d=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(d?.error?.message||`Gemini request failed (${r.status})`),{code:'GEMINI_PROVIDER_ERROR',status:r.status});const text=d?.candidates?.[0]?.content?.parts?.map(x=>x.text||'').join('')||'',parsed=parse(text);return parsed?.people||null}catch(e){console.warn('[SCAN_VERIFY] Gemini cross-check unavailable:',e?.message||e);return null}finally{clearTimeout(timer)}}
function compareModels(primary,secondary){if(!secondary)return{enabled:false,agreement:null,disagreements:[]};const disagreements=[];const max=Math.max(primary.length,secondary.length);for(let i=0;i<max;i++){const a=primary[i],b=secondary[i];if(!a||!b){disagreements.push({row:i+1,type:'row_count',primary:a||null,secondary:b||null});continue}const an=nameKey(a.n),bn=nameKey(b.n),ap=(a.p||[]).map(digits).filter(Boolean),bp=(b.p||[]).map(digits).filter(Boolean),sameNames=an===bn,samePhones=ap.length===bp.length&&ap.every(x=>bp.includes(x));if(!sameNames||!samePhones)disagreements.push({row:i+1,type:'transcription',name_match:sameNames,phone_match:samePhones,primary:{name:a.n,phones:a.p},secondary:{name:b.n,phones:b.p}})}return{enabled:true,agreement:disagreements.length===0,disagreements}}}
function addDisagreementFlags(primary,secondary){if(!secondary)return primary;const cmp=compareModels(primary,secondary);if(cmp.agreement)return primary;return primary.map((x,i)=>{const d=cmp.disagreements.find(z=>z.row===i+1);return d?{...x,f:[...new Set([...(x.f||[]),'cross_model_disagreement'])],e:`${x.e||''} Independent vision cross-check disagreed; human verification required.`.trim()}:x})}
function diagnosticLog(parts,merged,verification){try{console.log(`[SCAN_LAB] pipeline=${SCAN_PIPELINE_VERSION} regions=${parts.length} merged=${merged.length} crosscheck=${verification.enabled?'on':'off'} agreement=${verification.agreement}`);for(const p of parts)console.log(`[SCAN_LAB] region=${p.region} rows=${p.people.length} model=${p.model} request=${p.requestId}`);for(const x of merged)console.log(`[SCAN_LAB] row=${x.r} name=${JSON.stringify(x.n)} phones=${JSON.stringify(x.p)} flags=${JSON.stringify(x.f)}`)}catch{}}
function errorDetails(err){return{code:err?.code||'SCAN_FAILED,status':err?.status||null,provider:err?.provider||null,request_id:err?.requestId||null,retry_after:err?.retryAfter??null,reset_after_seconds:err?.resetAfterSeconds??null,cooldown_seconds:err?.cooldownSeconds??null}}
export function getScanAdmission(){return GROQ_KEY?{allowed:true,max_people_per_page:MAX,primary_output_tokens:PRIMARY.max_completion_tokens,verification_output_tokens:GEMINI_KEY?6000:0,safe_output_ceiling:PRIMARY.max_completion_tokens,adaptive:true,row_verified:'multi_region_plus_optional_independent_vision',orientation_resilient:true,reasoning:'Qwen high reasoning per overlapping region',pipeline_version:SCAN_PIPELINE_VERSION,model:PRIMARY.model,fallback_model:FALLBACK.model,crosscheck_model:GEMINI_KEY?GEMINI_MODEL:null,confidence_source:'regional_consensus+independent_crosscheck+database_validation'}:{allowed:false,code:'AI_NOT_CONFIGURED',message:'ARIA scan is not configured.'}}
function rateMessage(err){const n=Math.max(0,Number(err?.cooldownSeconds||err?.resetAfterSeconds||err?.retryAfter||0));if(!n)return'Groq vision is temporarily rate-limited. ARIA stopped before changing anything. Try again when the limit resets.';const m=Math.floor(n/60),s=Math.ceil(n%60);return`Groq vision is temporarily rate-limited. ARIA stopped before changing anything. Try again in about ${m?`${m} min${m===1?'':'s'}${s?` ${s} sec`:''}`:`${s} sec`}.`}
export async function callVisionWithRetry(img,_unused,onProgress=null,opt={}){if(!GROQ_KEY)throw Object.assign(new Error('GROQ_API_KEY is missing'),{code:'AI_NOT_CONFIGURED'});const image=normalizeImage(img),regions=await makeRegions(image.buffer),end=Date.now()+DEADLINE;onProgress?.(regions.length>1?'reading_overlapping_regions':'reading_page');let parts;try{parts=await Promise.all(regions.map((region,i)=>groqRegion(region,opt,i+1).then(x=>({...x,regionObj:region,pageHeight:regions.length?Math.max(...regions.map(r=>r.y1)):1}))))}catch(e){if(e?.code==='AI_RATE_LIMIT')throw Object.assign(e,{provider:'groq',userMessage:rateMessage(e)});throw e}const merged=mergeRegions(parts);if(!merged.length)throw Object.assign(new Error('Qwen vision found no readable register rows.'),{code:'AI_INVALID_RESPONSE'});onProgress?.(GEMINI_KEY?'cross_checking':'validating_extraction');const gemini=await geminiVerify(image.base64,merged,opt);const verification=compareModels(merged,gemini),finalPeople=addDisagreementFlags(merged,gemini);diagnosticLog(parts,finalPeople,verification);const payload={people:finalPeople};return{data:{choices:[{message:{content:JSON.stringify(payload)},finish_reason:'stop'}],usage:{prompt_tokens:0,completion_tokens:0,total_tokens:0}},verification:{passes:regions.length+(gemini?1:0),extracted:finalPeople.length,model_extracted:merged.length,mode:'overlapping_regions_plus_independent_crosscheck',primary_model:PRIMARY.model,model:PRIMARY.model,fallback_used:false,reviewed:!!gemini,agreement:verification.agreement,disagreements:verification.disagreements,regions:regions.length,crosscheck_model:gemini?GEMINI_MODEL:null},tokenPlan:{primary:PRIMARY.max_completion_tokens,region_count:regions.length,crosscheck:gemini?6000:0,safe_ceiling:PRIMARY.max_completion_tokens,recovery:'multi-region consensus; no silent correction'},vision:`hybrid-${PRIMARY.model}`,pipeline_version:SCAN_PIPELINE_VERSION,request_id:parts.map(x=>x.requestId).filter(Boolean).join(','),provider:'groq',model:PRIMARY.model,fallback_model:FALLBACK.model,rate_limit:parts.at(-1)?.rateLimit||null,diagnostic:{regions:parts.map(x=>({region:x.region,rows:x.people.length,request_id:x.requestId})),crosscheck:verification}}}
