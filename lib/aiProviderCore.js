// lib/aiProviderCore.js
import{getModelConfig}from'./modelRegistry';import{logAIUsage}from'./aiObserver';import crypto from'crypto';
const KEY=process.env.GROQ_API_KEY,MODEL=getModelConfig(process.env.GROQ_VISION_MODEL_KEY||'groq-qwen3.8-27b'),URL='https://api.groq.com/openai/v1/chat/completions';export const SCAN_PIPELINE_VERSION='v32-bounded-adaptive-register-extraction';const MAX=50,PRIMARY_TIMEOUT=12000,VERIFY_TIMEOUT=6500,DEADLINE=25000,T={mr:'Mr',mrs:'Mrs',ms:'Ms',miss:'Miss',bro:'Bro',brother:'Brother',sis:'Sis',sister:'Sister',pastor:'Pastor',past:'Past',pst:'Pst',rev:'Rev',reverend:'Reverend',dr:'Dr',doctor:'Doctor',elder:'Elder',deacon:'Deacon',deaconess:'Deaconess',bishop:'Bishop',apostle:'Apostle',chief:'Chief',evangelist:'Evangelist',prophet:'Prophet',minister:'Minister',canon:'Canon',father:'Father',archbishop:'Archbishop'},clean=x=>String(x??'').replace(/\s+/g,' ').trim(),fmt=x=>clean(x).split(/\s+/).map(p=>p.replace(/[A-Za-zÀ-ÖØ-öø-ÿ]+/g,w=>T[w.toLowerCase()]||w[0].toUpperCase()+w.slice(1).toLowerCase())).join(' '),phones=x=>{const p=x?.p??x?.phones??x?.phone;return Array.isArray(p)?p.map(clean).filter(Boolean).slice(0,2):p==null?[]:[clean(p)]};
function parse(x){if(typeof x!=='string')return null;const s=x.replace(/<think>[\s\S]*?<\/think>/gi,'').replace(/```json|```/gi,'').trim();try{return JSON.parse(s)}catch{}try{const m=s.match(/\{[\s\S]*\}/);return m?JSON.parse(m[0]):null}catch{return null}}
function compact(x){const a=Array.isArray(x?.people)?x.people:Array.isArray(x)?x:[],o=[],seen=new Set();for(let i=0;i<a.length&&o.length<MAX;i++){const r=a[i],n=clean(r?.n??r?.name),p=phones(r),rn=Number(r?.r),row=Number.isInteger(rn)&&rn>0&&rn<=MAX?rn:i+1;if(!n&&!p.length)continue;const k=`${row}|${n.toLowerCase()}|${p.join(',')}`;if(seen.has(k))continue;seen.add(k);o.push({name:fmt(n),raw_name:n,phone:p[0]||null,phones:p,rawPhones:p,confidence:0,name_confidence:0,phone_confidence:0,pair_confidence:0,row_number:row,model_row_number:Number.isInteger(rn)?rn:null})}return o}
const PROMPT=`NYEOCARE production register transcription. Return ONLY compact JSON {"people":[{"r":1,"n":"Sis Sandra Isichei","p":["08039579788"]}]}. Read the ORIGINAL image, not assumptions. FIRST determine true reading orientation; it may be upright, sideways, upside down or rotated 90/180/270 degrees. Then reconstruct logical top-to-bottom named-person order. Handle skew, perspective, curved binding, cropped edges, shadows, glare, folds, wrinkles, stains, dirty paper, faded ink, ink bleed and broken ruled lines as visual noise. A blank ruled line containing only a phone before the next name belongs to the PRECEDING named person. Pair phones by physical row structure, never list position or proximity alone. Read every phone digit exactly as visible: NEVER infer, repair, autocomplete, shift, borrow, substitute or normalize. Preserve leading 0 or +234. Support up to two phones per person. If genuinely unreadable, omit it rather than inventing it. Preserve visible prefixes/titles such as Sis, Sister, Bro, Brother, Mr, Mrs, Miss, Ms, Pastor, Rev, Reverend, Dr, Elder, Deacon, Bishop, Apostle and similar; preserve uncommon Nigerian, African and English names, hyphens and apostrophes. Ignore headers, totals, dates, notes, signatures, page numbers and unrelated numbers. Recheck first rows, middle rows, final two rows, continuation phones, every digit, every title and named-person count. Maximum ${MAX} people. JSON only.`;
const VERIFY=`Independently audit the ORIGINAL register image against the candidate. Determine orientation yourself. Check physical ruled-row ownership, blank continuation-phone lines, every phone digit, every visible title/name, and the first/middle/final rows. Never repair or invent digits. Never shift a phone to make it look plausible. Preserve uncommon Nigerian/African/English names. Return ONLY the corrected compact JSON {"people":[{"r":1,"n":"Name","p":["08012345678"]}]}.
CANDIDATE:`;
function rf(){return MODEL.supports_structured_output?{type:'json_schema',json_schema:{name:'scan_people',strict:true,schema:{type:'object',properties:{people:{type:'array',items:{type:'object',properties:{r:{type:'integer',minimum:1,maximum:MAX},n:{type:'string'},p:{type:'array',items:{type:'string'},maxItems:2}},required:['r','n','p'],additionalProperties:false}}},required:['people'],additionalProperties:false}}}}:{type:'json_object'}}
async function req(img,text,tokens,opt={}){const ac=new AbortController(),tm=Math.max(1000,Math.min(Number(opt.timeoutMs)||PRIMARY_TIMEOUT,Number(opt.remainingMs)||DEADLINE)),timer=setTimeout(()=>ac.abort(),tm),rid=crypto.randomUUID(),st=Date.now();try{const body={model:MODEL.model,messages:[{role:'user',content:[{type:'text',text},{type:'image_url',image_url:{url:`data:image/jpeg;base64,${img}`}}]}],temperature:.05,top_p:.8,max_completion_tokens:tokens,response_format:rf()};if(MODEL.supports_reasoning_effort){body.reasoning_effort='none';body.reasoning_format='hidden'}const r=await fetch(URL,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${KEY}`},body:JSON.stringify(body),signal:ac.signal}),d=await r.json().catch(()=>({}));await logAIUsage({organization_id:opt.organization_id,job_id:opt.job_id,request_id:rid,provider:'groq',model:MODEL.model,purpose:opt.purpose||'scan',prompt_version:SCAN_PIPELINE_VERSION,attempt:opt.attempt||1,latency_ms:Date.now()-st,input_tokens:Number(d?.usage?.prompt_tokens||0),output_tokens:Number(d?.usage?.completion_tokens||0),http_status:r.status,success:r.ok,finish_reason:d?.choices?.[0]?.finish_reason||null}).catch(()=>{});if(!r.ok)throw Object.assign(new Error(d?.error?.message||`Groq request failed (${r.status})`),{code:r.status===429?'AI_RATE_LIMIT':'AI_PROVIDER_ERROR',retryable:false,status:r.status});if(d?.choices?.[0]?.finish_reason==='length')throw Object.assign(new Error('ARIA could not safely finish reading this register.'),{code:'AI_OUTPUT_TRUNCATED',retryable:false});return d}catch(e){if(e?.name==='AbortError')throw Object.assign(new Error('Vision provider timed out'),{code:'AI_TIMEOUT',retryable:false});throw e}finally{clearTimeout(timer)}}
function suspicious(p){if(!p.length)return true;const names=new Set(p.map(x=>String(x.name||'').toLowerCase())),missing=p.filter(x=>!x.phones?.length).length,bad=p.some(x=>x.phones?.some(v=>{const d=String(v).replace(/\D/g,'');return d.length<10||d.length>15})),dupe=names.size<p.length;return missing>0||bad||dupe}
function compatible(a,b){if(!a.length||a.length!==b.length)return false;return a.every((x,i)=>Number(x.row_number)===Number(b[i].row_number)&&!!x.name&&!!b[i].name)}
export function getScanAdmission(){return KEY?{allowed:true,max_people_per_page:MAX,max_output_tokens:900,provider_output_ceiling:900,safe_output_ceiling:900,adaptive:true,row_verified:true,orientation_resilient:true,pipeline_version:SCAN_PIPELINE_VERSION}:{allowed:false,code:'AI_NOT_CONFIGURED',message:'ARIA scan is not configured.'}}
export async function callVisionWithRetry(img,_unused,onProgress=null,opt={}){if(!KEY)throw Object.assign(new Error('GROQ_API_KEY is missing'),{code:'AI_NOT_CONFIGURED',retryable:false});if(typeof img!=='string'||img.length<1000)throw Object.assign(new Error('Invalid scan image'),{code:'INVALID_IMAGE',retryable:false});const end=Date.now()+DEADLINE;onProgress?.('reading_page');const primary=await req(img,PROMPT,900,{...opt,purpose:'scan_primary',timeoutMs:PRIMARY_TIMEOUT,remainingMs:end-Date.now()});const first=compact(parse(primary?.choices?.[0]?.message?.content||''));if(!first.length)throw Object.assign(new Error('No people could be extracted from the register image'),{code:'NO_PEOPLE_EXTRACTED',retryable:false});let accepted=primary,people=first,mode='fast_primary',reviewed=null;if(suspicious(first)&&Date.now()+VERIFY_TIMEOUT<end){onProgress?.('auditing_rows');const candidate=JSON.stringify({people:first.map((p,i)=>({r:i+1,n:p.name,p:p.phones||[]}))});try{const second=await req(img,`${VERIFY}\n${candidate}`,700,{...opt,purpose:'scan_verification',attempt:2,timeoutMs:VERIFY_TIMEOUT,remainingMs:end-Date.now()});const secondPeople=compact(parse(second?.choices?.[0]?.message?.content||''));if(compatible(first,secondPeople)){accepted=second;people=secondPeople;reviewed=secondPeople;mode='verified_two_pass'}}catch{}}onProgress?.('finalizing_scan');return{data:{...accepted,choices:[{...(accepted.choices?.[0]||{}),message:{...(accepted.choices?.[0]?.message||{}),content:JSON.stringify({people})}}]},verification:{passes:reviewed?2:1,extracted:people.length,mode,reviewed:!!reviewed,reviewer_extracted:reviewed?.length||null},tokenPlan:{primary:900,verification:reviewed?700:null,recovery:null,min_output:520,safe_ceiling:900,provider_ceiling:900,max_people_per_page:MAX,adaptive:true,deduplicated:true,row_verified:!!reviewed,orientation_resilient:true,adversarial_review:!!reviewed},vision:'groq-adaptive-orientation-row-aware-name-phone',pipeline_version:SCAN_PIPELINE_VERSION,request_id:accepted?.id||null}}
