// lib/apiHelpers.js
// Canonical API authorization boundary.
import{getCurrentCareUser}from'./auth';
export function withOrg(handler){return async(req,res)=>{try{const user=await getCurrentCareUser(req);if(!user)return res.status(401).json({error:'Unauthorized'});if(!user.organization_id){console.error('[AUTH] User resolved without organization:',user.id);return res.status(403).json({error:'Organization access is not configured.'})}req.user=user;req.org={id:user.organization_id,name:user.organization_name};return handler(req,res)}catch(err){const code=err?.code||'AUTH_SERVICE_UNAVAILABLE';console.error('[AUTH] withOrg failure:',code,err?.message||err);return res.status(code==='AUTH_STORAGE_UNAVAILABLE'?503:500).json({error:code==='AUTH_STORAGE_UNAVAILABLE'?'Authentication storage is temporarily busy. Please try again in a moment.':'Authentication service unavailable.',code})}}}
export function withAdmin(handler){return withOrg(async(req,res)=>{if(!['owner','admin'].includes(req.user.role))return res.status(403).json({error:'Admin permissions required'});return handler(req,res)})}
