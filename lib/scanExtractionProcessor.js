// lib/scanExtractionProcessor.js
import pool from'./db';import crypto from'crypto';import{callVisionWithRetry,SCAN_PIPELINE_VERSION}from'./aiProvider';import{validateScanOutput,normalizeName}from'./scanValidation';import{INTERNAL_STATES,dbStatusFromInternal}from'./scanState';
const TERMINAL=new Set([INTERNAL_STATES.COMPLETED,INTERNAL_STATES.FAILED,INTERNAL_STATES.TIMED_OUT]);
async function state(client,id,nextState,progress,result,attempt,start){const now=new Date(),terminal=TERMINAL.has(nextState),status=dbStatusFromInternal(nextState),errorCode=terminal?(result?.error?.code||null):null,duration=terminal?Math.max(0,Date.now()-start):null;await client.query(`UPDATE scan_jobs SET status=$2,progress=$3,heartbeat=$4,last_progress_at=$4,attempt_count=$5,provider_used='groq',completed_at=CASE WHEN $6 THEN $4 ELSE completed_at END,duration_ms=CASE WHEN $6 THEN $7 ELSE duration_ms END,result=CASE WHEN $6 THEN $8::jsonb ELSE result END,error_code=CASE WHEN $6 THEN $9 ELSE error_code END WHERE id=$1`,[id,status,progress||null,now,attempt||0,terminal,duration,result==null?null:JSON.stringify(result),errorCode])}
function imageBuffer(b){return Buffer.from(String(b||'').replace(/^data:[^;]+;base64,/,'').replace(/\s/g,''),'base64')}
async function evidence(client,id,org,b64,hash){const b=imageBuffer(b64);if(!b.length||b.length>4000000)throw Object.assign(Error('Prepared scan image is invalid or too large.'),{code:'INVALID_IMAGE'});await client.query(`INSERT INTO scan_evidence(organization_id,scan_job_id,mime_type,image_hash,image_data) VALUES($1,$2,'image/jpeg',$3,$4) ON CONFLICT(scan_job_id) DO UPDATE SET image_hash=EXCLUDED.image_hash,image_data=EXCLUDED.image_data,mime_type=EXCLUDED.mime_type`,[org,id,hash,b])}
async function active(client,id){const q=await client.query(`SELECT status,error_code FROM scan_jobs WHERE id=$1 FOR UPDATE`,[id]);if(!q.rows.length)throw Error('Scan job not found.');if(q.rows[0].error_code==='SCAN_CANCELLED'||q.rows[0].status==='failed')throw Object.assign(Error('SCAN_CANCELLED'),{code:'SCAN_CANCELLED',userMessage:'Scan cancelled. Your existing data is safe.'});return q.rows[0]}
function phones(person){return(Array.isArray(person.phones)?person.phones:person.phone?[person.phone]:[]).slice(0,2).map((normalized,i)=>({raw:(person.rawPhones||[])[i]||normalized,normalized,source:'scan',index:i+1}))}
function aiUserMessage(e){const c=e?.code;if(c==='AI_RATE_LIMIT'){const wait=Math.max(5,Number(e?.cooldownSeconds)||Number(e?.resetAfterSeconds)||60),m=Math.floor(wait/60),s=wait%60,t=m?`${m} min${m===1?'':'s'}${s?` ${s} sec`:''}`:`${s} sec`;return`Too many scan requests right now. ARIA needs ${t} to cool down before the vision service can continue. Nothing was changed. Please try again after that.`}if(c==='AI_TIMEOUT')return'ARIA took too long to finish reading this register. Nothing was changed. Please try again with the page fully visible and steady.';if(c==='AI_OUTPUT_TRUNCATED')return'ARIA reached the safe reading limit before the register was complete. Nothing was changed. Please use a clearer or more tightly framed register.';if(c==='SCAN_AUDIT_DISAGREEMENT')return'ARIA found a disagreement between its independent register readings, so she refused to save uncertain people or phone numbers. Nothing was changed. Please retake the register clearly and try again.';if(c==='SCAN_AUDIT_EMPTY')return'ARIA could not get a second safe reading of this register. Nothing was changed. Please try again with a clearer image.';if(c==='NO_PEOPLE_EXTRACTED')return'ARIA could not confidently identify people in this register. Nothing was changed. Please make sure the names and phone numbers are visible.';if(c==='AI_NOT_CONFIGURED')return'ARIA scan is not configured on the server yet. Nothing was changed.';if(c==='INVALID_IMAGE')return'ARIA could not safely read this image. Nothing was changed. Please retake it as a clear JPG or PNG.';if(c==='AI_PROVIDER_ERROR'){const s=Number(e?.status||e?.providerStatus);if(s===401)return'ARIA could not authenticate with the vision service. Nothing was changed. The scan service needs its server credentials checked.';if(s===403)return'ARIA is not permitted to use the current vision service. Nothing was changed. The scan service configuration needs attention.';if(s===413)return'This register image is too large for the vision service. Nothing was changed. Please use a smaller, clearer photo.';if(s===422)return'The vision service could not process this scan request. Nothing was changed. Please try the scan again.';if(s>=500)return'The vision service is temporarily unavailable. Nothing was changed. Please try again shortly.';return'ARIA could not reach the vision service safely. Nothing was changed. Please try again.'}return'ARIA could not safely read this register. Nothing was changed.'}
function resultError(e){return{error:{stage:e?.stage||'scan',code:e?.code||'SCAN_FAILED',message:e?.message||'Scan failed.',userMessage:e?.userMessage||'ARIA could not safely complete this scan. Nothing was changed.',status:e?.status||e?.providerStatus||null,retry_after_seconds:e?.retryAfter??null,reset_after_seconds:e?.resetAfterSeconds??null,cooldown_seconds:e?.cooldownSeconds??null,request_id:e?.requestId||null,finish_reason:e?.finishReason||null,provider_usage:e?.usage||null,rows:e?.rows||null}}}
export async function processScanExtraction(jobId,imageBase64,orgId,programName,options={}){const start=Date.now(),client=await pool.connect();let tx=false,stage='acquire_client';try{stage='preparing_image';await state(client,jobId,INTERNAL_STATES.ANALYSING,'preparing_image',null,0,start);const hash=crypto.createHash('sha256').update(imageBase64).digest('hex');stage='recording_image_hash';await client.query(`UPDATE scan_jobs SET image_hash=$1,started_at=COALESCE(started_at,NOW()),heartbeat=NOW(),last_progress_at=NOW() WHERE id=$2 AND organization_id=$3`,[hash,jobId,orgId]);stage='saving_evidence';await evidence(client,jobId,orgId,imageBase64,hash);stage='entering_vision';await state(client,jobId,INTERNAL_STATES.EXTRACTING,'reading_page',null,0,start);let vision;try{stage='vision_request';vision=await callVisionWithRetry(imageBase64,null,p=>state(client,jobId,INTERNAL_STATES.EXTRACTING,p,null,1,start).catch(err=>console.error('[SCAN] progress state failed',jobId,p,err?.code||err?.message||err)),{organization_id:orgId,job_id:jobId,purpose:'scan_extraction',program_name:programName})}catch(e){const r=resultError({...e,stage:e?.stage||stage,userMessage:aiUserMessage(e)});await state(client,jobId,INTERNAL_STATES.FAILED,'failed',r,1,start);return r}stage='reading_result';const raw=vision?.data?.choices?.[0]?.message?.content;if(!raw)throw Object.assign(Error('Vision returned no content.'),{code:'EMPTY_AI_RESPONSE'});stage='validating';await state(client,jobId,INTERNAL_STATES.VALIDATING,'validating',null,vision.attempt||1,start);const validation=await validateScanOutput(raw,orgId,programName,jobId,{evaluation:false});if(!validation.valid){const r=resultError(Object.assign(Error(validation.error),{code:'INVALID_AI_RESPONSE',stage}));await state(client,jobId,INTERNAL_STATES.FAILED,'failed',r,vision.attempt||1,start);return r}stage='building_memory';await state(client,jobId,INTERNAL_STATES.SAVING,'building_memory',null,vision.attempt||1,start);stage='transaction_begin';await client.query('BEGIN');tx=true;stage='transaction_lock';await active(client,jobId);const created=[],matched=[],review=[];for(const person of validation.people){stage='matching_people';const ph=phones(person),valid=ph.filter(x=>x.normalized),key=normalizeName(person.display_name).replace(/[^a-z0-9]/g,'');let existing=null;if(valid.length){const q=await client.query(`SELECT id,display_name,type,phone,phone_numbers FROM people WHERE organization_id=$1 AND status='active' AND lower(regexp_replace(coalesce(display_name,''),'[^a-z0-9]','','g'))=$2 AND EXISTS(SELECT 1 FROM jsonb_array_elements(coalesce(phone_numbers,'[]'::jsonb)) v WHERE regexp_replace(coalesce(v->>'normalized',v->>'raw',''),'\\D','','g')=ANY($3::text[])) LIMIT 1`,[orgId,key,valid.map(x=>String(x.normalized).replace(/\D/g,''))]);existing=q.rows[0]||null}const metadata={scan_pipeline_version:SCAN_PIPELINE_VERSION,row_number:person.row_number,model_row_number:person.model_row_number??null,scan_evidence_job_id:jobId,vision_provider:'groq',phone_numbers:ph,raw_name:person.raw_name||null,raw_phones:person.rawPhones||[],program_name:programName,extraction_only:true,needs_review:!!validation.needsReview.find(x=>Number(x.row_number)===Number(person.row_number))};const rr=validation.needsReview.find(x=>Number(x.row_number)===Number(person.row_number));if(rr)review.push(rr);if(existing){stage='updating_person';await client.query(`UPDATE people SET last_scan_job_id=$1,metadata=coalesce(metadata,'{}'::jsonb)||$2::jsonb WHERE id=$3`,[jobId,JSON.stringify(metadata),existing.id]);matched.push({id:existing.id,name:existing.display_name,phones:existing.phone_numbers||[],row_number:person.row_number});continue}stage='creating_person';const type='visitor',insert=await client.query(`INSERT INTO people(organization_id,first_name,last_name,display_name,phone,phone_numbers,type,status,confidence,source,created_by,last_scan_job_id,quarantine_reason,living_truth,metadata) VALUES($1,$2,$3,$4,$5,$6,$7,'active',0,'scan',$8,$9,$10,$11,$12) RETURNING id,display_name,phone,phone_numbers,type`,[orgId,person.first_name,person.last_name,person.display_name,valid[0]?.normalized||null,JSON.stringify(ph),type,options.actorId||null,jobId,rr?'Scan extraction needs confirmation.':null,JSON.stringify(rr?{status:'needs_decision',source:'scan',updated_at:new Date().toISOString()}:{status:'alive',source:'scan',updated_at:new Date().toISOString()}),JSON.stringify(metadata)]);created.push({...insert.rows[0],row_number:person.row_number})}stage='building_result';const out={status:'ok',scan_pipeline_version:SCAN_PIPELINE_VERSION,new_members:created.length,updated:matched.length,duplicates:matched,needs_review:review,needs_review_count:review.length,rejected:validation.rejected,total_extracted:validation.total_extracted,total_valid:validation.total_valid,register_mode:'extract_only',verification:vision.verification,evidence:{available:true,job_id:jobId},layout:{provider:'groq-direct-vision',rows:validation.total_extracted},summary:`ARIA extracted ${validation.total_extracted} people, remembered ${created.length+matched.length}, and flagged ${review.length} rows for later review.`};stage='committing_state';await active(client,jobId);await state(client,jobId,INTERNAL_STATES.COMPLETED,'complete',out,vision.attempt||1,start);stage='commit';await client.query('COMMIT');tx=false;return out}catch(e){if(tx)try{await client.query('ROLLBACK')}catch{}const r=resultError({...e,stage:e?.stage||stage});console.error('[SCAN] failed',jobId,r.error);try{await state(client,jobId,INTERNAL_STATES.FAILED,'failed',r,e?.attempt||0,start)}catch(err){console.error('[SCAN] terminal state write failed',jobId,err?.code||err?.message||err)}return r}finally{client.release()}}
