// lib/scanGemini.js
import crypto from'crypto';import sharp from'sharp';import pool from'./db';import normalizePhone from'./phoneUtils';import{geminiRequest,geminiOutput}from'./aiProviders/gemini';import{logAIUsage}from'./aiObserver';
export const VERSION='v2-gemini38-physical-row-fixed-input';const MODEL='gemini-3.8-flash',TIMEOUT=120000,MAXTOK=6000,MAX=50;
const clean=x=>String(x??'').replace(/\s+/g,' ').trim(),digits=x=>String(x||'').replace(/\D/g,''),norm=x=>clean(x).toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z\s'\-]/g,' ').replace(/\s+/g,' ').trim();
function decode(x){let s=String(x||'').trim().replace(/^data:[^;]+;base64,/i,'').replace(/\s/g,'').replace(/-/g,'+').replace(/_/g,'/');if(!s||s.length%4===1)throw Object.assign(Error('Invalid image.'),{code:'INVALID_IMAGE'});const b=Buffer.from(s,'base64');if(!b.length)throw Object.assign(Error('Invalid image.'),{code:'INVALID_IMAGE'});const mime=b[0]===255&&b[1]===216?'image/jpeg':b[0]===137&&b[1]===80?'image/png':b.slice(0,4).toString()==='RIFF'&&b.slice(8,12).toString()==='WEBP'?'image/webp':null;if(!mime)throw Object.assign(Error('Unsupported image format.'),{code:'INVALID_IMAGE'});return{b,mime}}
export function getCanonicalImageHash(x){return crypto.createHash('sha256').update(decode(x).b).digest('hex')}
async function prep(b){const s=sharp(b).rotate(),m=await s.metadata(),w=Number(m.width),h=Number(m.height);if(!Number.isInteger(w)||!Number.isInteger(h)||w<1||h<1)throw Object.assign(Error('ARIA could not read the image dimensions.'),{code:'INVALID_IMAGE'});const x=await s.resize({width:Math.min(3500,w),withoutEnlargement:true}).jpeg({quality:96,mozjpeg:true}).toBuffer(),mm=await sharp(x).metadata();if(!Number.isInteger(mm.width)||!Number.isInteger(mm.height))throw Object.assign(Error('ARIA could not safely prepare the image.'),{code:'INVALID_IMAGE'});return x.toString('base64')}
const schema={type:'object',additionalProperties:false,properties:{rows:{type:'array',items:{type:'object',additionalProperties:false,properties:{row:{type:'integer',minimum:1,maximum:MAX},name:{type:'string'},phones:{type:'array',maxItems:2,items:{type:'object',additionalProperties:false,properties:{raw:{type:'string'},digits:{type:'string'},certain:{type:'boolean'}},required:['raw','digits','certain']}},continuation:{type:'boolean'},uncertain:{type:'boolean'},notes:{type:'string'}},required:['row','name','phones','continuation','uncertain','notes']}}},required:['rows']};
const prompt=`NYEOCARE register vision benchmark. Read the photographed handwritten register as a physical document, not as a text blob. PRIMARY objective: exact NAME + PHONE NUMBER + PHYSICAL ROW OWNERSHIP. First understand the page geometry and count physical person rows. Then read every row top-to-bottom.\n\nNever invent, repair, normalize, pad, truncate, transpose, borrow or infer a digit. Preserve the visible digit sequence in raw/digits and set certain=false when any digit is visually uncertain. A person may have one or two phone numbers. A blank/continuation line directly beneath a person can contain that same person's second number; use geometry, alignment, arrows, brackets and continuation marks. Never attach a neighbor's number merely because it makes a valid phone. Never replace an unfamiliar name with a familiar one. Preserve visible spelling and titles. Ignore blank/non-person lines. For a continuation row use name="" and continuation=true.\n\nBefore finalizing, re-check every digit left-to-right and right-to-left, every name, row count, and every name-phone relationship against the pixels. Current pixels outrank plausibility and learned context. Output ONLY JSON matching the supplied schema.`;
async function vision(base64,org,id){if(!process.env.GEMINI_API_KEY)throw Object.assign(Error('Gemini is not configured.'),{code:'AI_NOT_CONFIGURED'});const started=Date.now(),body={model:MODEL,input:[{type:'text',text:prompt},{type:'image',data:base64,mime_type:'image/jpeg'}],generation_config:{thinking_level:process.env.GEMINI_SCAN_THINKING_LEVEL||'medium',max_output_tokens:MAXTOK},response_format:{type:'text',mime_type:'application/json',schema}};const result=await geminiRequest(body,{timeoutMs:TIMEOUT,retries:0});const latency=Date.now()-started;const usage=result.data?.usage||{};await logAIUsage({organization_id:org,job_id:id,request_id:result.data?.id||crypto.randomUUID(),model_key:'gemini-3.8-flash',provider:'gemini',model:MODEL,purpose:'scan',prompt_version:VERSION,attempt:1,latency_ms:latency,input_tokens:Number(usage.total_input_tokens||usage.input_tokens||0),output_tokens:Number(usage.total_output_tokens||usage.output_tokens||0),total_tokens:Number(usage.total_tokens||0),http_status:result.response.status,success:result.response.ok,finish_reason:'stop'}).catch(()=>{});if(!result.response.ok){const s=result.response.status,msg=result.data?.error?.message||'Gemini could not safely complete the scan.';if(s===401||s===403)throw Object.assign(Error('Gemini rejected the configured credential.'),{code:'AI_AUTH_ERROR',status:s,provider_message:msg});if(s===429)throw Object.assign(Error('Gemini is temporarily rate-limited.'),{code:'AI_RATE_LIMIT',status:s,provider_message:msg});throw Object.assign(Error(msg),{code:'AI_PROVIDER_ERROR',status:s,provider_message:msg})}const text=geminiOutput(result.data);if(!text)throw Object.assign(Error('Gemini returned no register data.'),{code:'INVALID_AI_RESPONSE'});let data;try{data=JSON.parse(text)}catch{throw Object.assign(Error('Gemini returned invalid register JSON.'),{code:'INVALID_AI_RESPONSE'})}if(!Array.isArray(data.rows)||!data.rows.length)throw Object.assign(Error('Gemini did not establish physical register rows.'),{code:'INVALID_AI_RESPONSE'});return data.rows}
function reconstruct(rows){const out=[];let prev=null;for(let i=0;i<rows.length;i++){const r=rows[i]||{},name=clean(r.name||''),phones=(Array.isArray(r.phones)?r.phones:[]).map(p=>({raw:clean(p?.raw||p?.digits||''),digits:digits(p?.digits||p?.raw||''),certain:p?.certain!==false})).filter(p=>p.digits.length>=7);if((r.continuation||!name)&&phones.length&&prev&&prev.phones.length<2){prev.phones.push(...phones.slice(0,2-prev.phones.length));prev.source_rows.push(Number(r.row)||i+1);if(r.uncertain)prev.flags.push('phone_uncertain');continue}if(!name&&!phones.length)continue;const parts=name.split(/\s+/).filter(Boolean),title=/^(sis(ter)?|bro(ther)?|mr?s?|past(or)?|dr|rev|elder|deacon(ess)?)$/i.test(parts[0]||''),v=title?parts.slice(1):parts,flags=[];if(!name)flags.push('name_missing');if(!phones.length)flags.push('phone_missing');if(r.uncertain||phones.some(p=>!p.certain))flags.push('ai_uncertain');if(phones.some(p=>p.digits.length<10||p.digits.length>15))flags.push('phone_unreadable');const p={row_number:Number(r.row)||i+1,name,first_name:v[0]||'',last_name:v.slice(1).join(' '),phone:phones[0]?.raw||null,phones:phones.slice(0,2),flags,source_rows:[Number(r.row)||i+1],notes:clean(r.notes||'')};out.push(p);prev=p}return out}
async function save(c,org,id,actor,rows){const created=[],known=[],review=[];for(const p of rows){const normalized=p.phones.map(x=>normalizePhone(x.raw||x.digits)).filter(Boolean),unsafe=p.flags.length||!p.name||!normalized.length;if(unsafe){review.push({...p,phones:normalized});continue}const q=await c.query(`select id,display_name,phone_numbers from people where organization_id=$1 and status='active' and lower(regexp_replace(coalesce(display_name,''),'[^a-z0-9]','','g'))=lower(regexp_replace($2,'[^a-z0-9]','','g')) limit 1`,[org,norm(p.name)]);if(q.rows[0]){const e=q.rows[0],ep=(e.phone_numbers||[]).map(x=>normalizePhone(x?.normalized||x?.raw||x)).filter(Boolean);if(normalized.some(x=>ep.includes(x)))known.push({row_number:p.row_number,name:p.name,matched_person:{id:e.id,name:e.display_name,phones:ep},match_type:'exact_name_phone'});else review.push({...p,phones:normalized,flags:['existing_name_phone_conflict']});continue}const q2=await c.query(`select id,display_name from people where organization_id=$1 and status='active' and exists(select 1 from jsonb_array_elements(coalesce(phone_numbers,'[]'::jsonb)) v where regexp_replace(coalesce(v->>'normalized',v->>'raw',''),'\\D','','g')=any($2::text[])) limit 1`,[org,normalized]);if(q2.rows[0]){review.push({...p,phones:normalized,flags:['existing_phone_name_conflict']});continue}const jp=JSON.stringify(normalized.map(x=>({raw:x,normalized:x,source:'scan_gemini38'}))),ins=await c.query(`insert into people(organization_id,first_name,last_name,display_name,phone,phone_numbers,type,status,confidence,source,created_by,last_scan_job_id,living_truth,metadata) values($1,$2,$3,$4,$5,$6,'visitor','active',100,'scan',$7,$8,$9,$10) returning id,display_name,phone,phone_numbers,type`,[org,p.first_name,p.last_name,p.name,normalized[0],jp,actor,id,JSON.stringify({status:'alive',source:'scan',updated_at:new Date().toISOString()}),JSON.stringify({scan_pipeline_version:VERSION,row_number:p.row_number,source_rows:p.source_rows})]);created.push({...ins.rows[0],row_number:p.row_number})}return{created,known,review}}
export async function runScanGemini(id,image,org,actor,program='GIBEON'){const c=await pool.connect();let tx=false;try{const hash=getCanonicalImageHash(image),im=decode(image),base64=await prep(im.b);await c.query(`update scan_jobs set status='processing',progress='preparing_page',provider_used='gemini',started_at=coalesce(started_at,now()),heartbeat=now(),last_progress_at=now(),image_hash=$1,program_name=$2,scan_pipeline_version=$3,error_code=null,error_message=null where id=$4 and organization_id=$5`,[hash,program,VERSION,id,org]);await c.query(`update scan_jobs set progress='gemini_reading',heartbeat=now(),last_progress_at=now(),attempt_count=attempt_count+1 where id=$1 and organization_id=$2 and status='processing'`,[id,org]);const raw=await vision(base64,org,id),people=reconstruct(raw);if(people.length<1||people.length>MAX)throw Object.assign(Error('Gemini could not establish a stable set of physical rows.'),{code:'INVALID_AI_RESPONSE'});await c.query('begin');tx=true;const lock=await c.query(`select status from scan_jobs where id=$1 and organization_id=$2 for update`,[id,org]);if(lock.rows[0]?.status!=='processing')throw Object.assign(Error('Scan state changed.'),{code:'SCAN_STATE_CONFLICT'});const saved=await save(c,org,id,actor,people),result={summary:{extracted:people.length,new_people:saved.created.length,already_known:saved.known.length,duplicates:0,needs_review:saved.review.length},extracted:people,created:saved.created,known:saved.known,review:saved.review,scan_pipeline_version:VERSION,verification:{mode:'gemini-3.8-flash-physical-row-reconstruction',passes:1,geometry:true,continuation_rows:true,model:MODEL}};const f=await c.query(`update scan_jobs set status='complete',progress='complete',result=$1::jsonb,error_code=null,error_message=null,completed_at=now(),duration_ms=greatest(0,round(extract(epoch from(now()-started_at))*1000)::int),heartbeat=now(),last_progress_at=now() where id=$2 and organization_id=$3 and status='processing' returning id`,[JSON.stringify(result),id,org]);if(!f.rows.length)throw Object.assign(Error('Scan state changed before completion.'),{code:'SCAN_STATE_CONFLICT'});await c.query('commit');tx=false;return result}catch(e){if(tx){await c.query('rollback').catch(()=>{});tx=false}const code=e?.code||'SCAN_FAILED',msg=code==='AI_NOT_CONFIGURED'?'Gemini is not configured on this deployment.':code==='AI_AUTH_ERROR'?'Gemini rejected the configured credential.':code==='AI_RATE_LIMIT'?'Gemini is temporarily rate-limited. ARIA will cool down and retry.':code==='AI_PROVIDER_ERROR'&&e?.status?'Gemini returned an error from the provider. Please try again.':'Gemini could not safely complete this scan. Nothing was changed.';await c.query(`update scan_jobs set status='failed',progress='failed',error_code=$1,error_message=$2,result=$3::jsonb,completed_at=now(),duration_ms=greatest(0,round(extract(epoch from(now()-started_at))*1000)::int),heartbeat=now(),last_progress_at=now() where id=$4 and organization_id=$5 and status in('pending','processing')`,[code,msg,JSON.stringify({error:{code,userMessage:msg,provider_status:e?.status||null,provider_message:e?.provider_message||null}}),id,org]).catch(()=>{});throw e}finally{c.release()}}
