import{withAdmin}from'../../../lib/apiHelpers';import{runCareCycle}from'../../../lib/aria/careCycle';
export default withAdmin(async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const result=await runCareCycle(req.org.id,{force:req.body?.force===true});if(result.failed)return res.status(207).json({ok:false,...result});return res.status(200).json({ok:true,...result});}catch(e){console.error('[ARIA] cycle',e);return res.status(500).json({error:'Unable to refresh ARIA care intelligence.'});}});
