export default function handler(req,res){return res.status(410).json({error:'Legacy attendance group-claim endpoint disabled. The current attendance flow uses session assignments.',code:'LEGACY_ATTENDANCE_ENDPOINT'});}
