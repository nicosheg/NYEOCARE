export default function handler(req,res){return res.status(410).json({error:'Legacy attendance-groups endpoint disabled. The current attendance flow uses session sections.',code:'LEGACY_ATTENDANCE_ENDPOINT'});}
