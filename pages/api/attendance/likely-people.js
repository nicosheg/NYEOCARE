export default function handler(req,res){return res.status(410).json({error:'Legacy likely-people endpoint disabled. The current attendance flow does not expose user attendance history through this route.',code:'LEGACY_ATTENDANCE_ENDPOINT'});}
