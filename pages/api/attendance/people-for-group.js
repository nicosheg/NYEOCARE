export default function handler(req,res){return res.status(410).json({error:'Legacy group people endpoint disabled. The current attendance flow uses organization-scoped session people.',code:'LEGACY_ATTENDANCE_ENDPOINT'});}
