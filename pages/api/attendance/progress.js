export default function handler(req,res){return res.status(410).json({error:'Legacy attendance progress endpoint disabled. The current attendance flow reads live attendance from the canonical session surface.',code:'LEGACY_ATTENDANCE_ENDPOINT'});}
