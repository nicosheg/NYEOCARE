export default function handler(req,res){return res.status(410).json({error:'Legacy session-people endpoint disabled. The current attendance flow uses the canonical people endpoint.',code:'LEGACY_ATTENDANCE_ENDPOINT'});}
