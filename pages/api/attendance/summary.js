export default function handler(req,res){return res.status(410).json({error:'Legacy attendance summary endpoint disabled. The current attendance flow uses ARIA session processing.',code:'LEGACY_ATTENDANCE_ENDPOINT'});}
