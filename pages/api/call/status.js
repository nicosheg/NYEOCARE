export default function handler(req,res){return res.status(410).json({error:'Legacy call webhook is disabled. The current ARIA care pipeline does not use this endpoint.',code:'LEGACY_CALL_ENDPOINT'});}
