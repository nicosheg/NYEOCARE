// pages/api/care-queue.js
import pool from'../../lib/db';import{withOrg}from'../../lib/apiHelpers';
function nextAction(type,action){if(action){return({SEND_MESSAGE:'Reach out personally',REQUEST_REVIEW:'Review this person',ESCALATE:'Give this person closer attention',MARK_ATTENDANCE:'Review attendance',SCAN:'Review the latest scan',DO_NOTHING:'Keep observing'})[action.type]||'Review this signal'}return({NEW_PERSON:'Welcome this person',UNUSUAL_ABSENCE:'Check in with this person',LOW_ENGAGEMENT:'Reach out personally',ATTENDANCE_CHANGE:'Review their recent attendance',CARE_RISK:'Give this person closer attention',POSSIBLE_DUPLICATE:'Review this person',PATTERN:'Review the emerging pattern'})[type]||'Review this signal'}
function signalText(o){const e=o?.evidence||{};if(e.inference)return e.inference;return({NEW_PERSON:'A new person was discovered.',UNUSUAL_ABSENCE:'A change from this person’s usual attendance pattern was noticed.',LOW_ENGAGEMENT:'Engagement appears to be changing.',ATTENDANCE_CHANGE:'A change in attendance was detected.',CARE_RISK:'ARIA detected a care-related signal.',POSSIBLE_DUPLICATE:'This person may need a record review.',PATTERN:'ARIA detected an emerging pattern.'})[o?.type]||'ARIA detected a meaningful signal.'}
async function handler(req,res){if(req.method==='POST')return report(req,res);if(req.method!=='GET'){res.setHeader('Allow','GET, POST');return res.status(405).json({error:'Method not allowed'})}const orgId=req.org.id;try{const result=await pool.query(`WITH recent_sessions AS(SELECT s.id,s.started_at,s.closed_at,s.name,s.service_type,COALESCE(NULLIF(s.service_type,''),'weekday:'||EXTRACT(ISODOW FROM s.started_at)::int)service_key,ROW_NUMBER()OVER(PARTITION BY COALESCE(NULLIF(s.service_type,''),'weekday:'||EXTRACT(ISODOW FROM s.started_at)::int)ORDER BY s.started_at DESC)rn FROM sessions s WHERE s.organization_id=$1 AND s.status='closed' AND s.started_at IS NOT NULL),latest_sessions AS(SELECT*FROM recent_sessions WHERE rn=1),person_session AS(SELECT p.id person_id,p.first_name,p.last_name,p.phone,rs.id session_id,rs.started_at,rs.name,rs.service_type,rs.service_key,COUNT(rs.id)FILTER(WHERE rs.rn<=4)::int learning_sessions,COUNT(rs.id)FILTER(WHERE rs.rn<=4 AND EXISTS(SELECT 1 FROM attendance_records ar WHERE ar.organization_id=$1 AND ar.people_id=p.id AND ar.session_id=rs.id AND ar.present=true AND ar.confirmed=true))::int attended_count,COALESCE(MIN(rs.rn)FILTER(WHERE rs.rn<=4 AND EXISTS(SELECT 1 FROM attendance_records ar WHERE ar.organization_id=$1 AND ar.people_id=p.id AND ar.session_id=rs.id AND ar.present=true AND ar.confirmed=true)),5)-1::int consecutive_misses FROM people p CROSS JOIN recent_sessions rs WHERE p.organization_id=$1 AND p.status='active' AND rs.rn<=4 GROUP BY p.id,p.first_name,p.last_name,p.phone,rs.service_key,rs.id,rs.started_at,rs.name,rs.service_type),current_people AS(SELECT ps.* FROM person_session ps JOIN latest_sessions ls ON ls.id=ps.session_id),contacts AS(SELECT pc.person_id,MAX(pc.occurred_at)last_contact_at FROM person_communications pc WHERE pc.organization_id=$1 GROUP BY pc.person_id),contexts AS(SELECT c.person_id,MAX(c.ends_on)FILTER(WHERE c.kind IN('travel','temporary_unavailable')AND(c.ends_on IS NULL OR c.ends_on>CURRENT_DATE))suppressed_until,MAX(c.service_type)FILTER(WHERE c.kind='preferred_service'AND c.ends_on IS NULL)preferred_service FROM aria_care_contexts c WHERE c.organization_id=$1 AND(c.ends_on IS NULL OR c.ends_on>CURRENT_DATE) GROUP BY c.person_id),attendance_items AS(SELECT cp.*,COALESCE(ct.last_contact_at,pc.last_meaningful_event)last_contact_at,ctx.suppressed_until,ctx.preferred_service,CASE WHEN cp.learning_sessions<4 THEN'medium' WHEN cp.consecutive_misses>=3 AND(COALESCE(ct.last_contact_at,pc.last_meaningful_event)IS NULL OR COALESCE(ct.last_contact_at,pc.last_meaningful_event)<NOW()-INTERVAL '21 days')THEN'high' WHEN cp.consecutive_misses>=2 THEN'medium' WHEN cp.attended_count::numeric/GREATEST(cp.learning_sessions,1)>=0.75 THEN'medium' ELSE'low' END priority FROM current_people cp LEFT JOIN contacts ct ON ct.person_id=cp.person_id LEFT JOIN contexts ctx ON ctx.person_id=cp.person_id LEFT JOIN LATERAL(SELECT MAX(em.last_meaningful_event)last_meaningful_event FROM engagement_metrics em WHERE em.organization_id=$1 AND em.person_id=cp.person_id)pc ON true WHERE cp.attended_count>0 AND NOT EXISTS(SELECT 1 FROM attendance_records ar WHERE ar.organization_id=$1 AND ar.people_id=cp.person_id AND ar.session_id=cp.session_id AND ar.present=true AND ar.confirmed=true)AND(ctx.suppressed_until IS NULL)AND(ctx.preferred_service IS NULL OR ctx.preferred_service=cp.service_key)),latest_obs AS(SELECT*FROM(SELECT o.*,ROW_NUMBER()OVER(PARTITION BY o.person_id ORDER BY o.attention_score DESC,o.detected_at DESC)rn FROM aria_observations o WHERE o.organization_id=$1 AND o.status='active'AND(o.expires_at IS NULL OR o.expires_at>NOW()))x WHERE rn=1),pending_action AS(SELECT*FROM(SELECT a.*,ROW_NUMBER()OVER(PARTITION BY a.person_id ORDER BY CASE a.priority WHEN'critical'then 4 WHEN'high'then 3 WHEN'medium'then 2 WHEN'low'then 1 ELSE 0 END DESC,a.proposed_at ASC)rn FROM aria_actions a WHERE a.organization_id=$1 AND a.status IN('proposed','approved'))x WHERE rn=1)SELECT p.id person_id,p.first_name,p.last_name,p.phone,o.id observation_id,o.type observation_type,o.confidence,o.severity,o.urgency,o.attention_score,o.evidence,o.detected_at,a.id action_id,a.type action_type,a.status action_status,a.priority action_priority,a.action_metadata,aps.engagement_state,aps.care_state,aps.relationship_state,aps.followup_state,aps.attention_level,COALESCE(aps.open_observation_count,0)open_observation_count,COALESCE(aps.open_action_count,0)open_action_count,aps.updated_at state_updated_at,ai.session_id care_session_id,ai.started_at care_session_at,ai.name care_session_name,ai.service_type care_service_type,ai.service_key care_service_key,ai.attended_count care_prior_attendance,ai.learning_sessions care_learning_sessions,ai.consecutive_misses care_consecutive_misses,ROUND(ai.attended_count::numeric/GREATEST(ai.learning_sessions,1),3)care_attendance_rate,ai.priority care_priority,ai.last_contact_at care_last_contact_at FROM people p LEFT JOIN latest_obs o ON o.person_id=p.id LEFT JOIN pending_action a ON a.person_id=p.id LEFT JOIN aria_person_state aps ON aps.person_id=p.id AND aps.organization_id=p.organization_id LEFT JOIN attendance_items ai ON ai.person_id=p.id WHERE p.organization_id=$1 AND p.status='active'AND(o.id IS NOT NULL OR a.id IS NOT NULL OR ai.person_id IS NOT NULL OR COALESCE(aps.open_observation_count,0)>0 OR COALESCE(aps.open_action_count,0)>0)ORDER BY CASE COALESCE(a.priority,o.severity,ai.priority,aps.attention_level,'low')WHEN'critical'then 4 WHEN'high'then 3 WHEN'medium'then 2 ELSE 1 END DESC,COALESCE(o.attention_score,0)DESC,COALESCE(ai.consecutive_misses,0)DESC,COALESCE(a.proposed_at,o.detected_at,ai.care_session_at)ASC LIMIT 100`,[orgId]);return res.status(200).json(result.rows.map(row=>{const attendance=!!row.care_session_id,rate=Number(row.care_attendance_rate)||0,misses=Number(row.care_consecutive_misses)||0,learning=Number(row.care_learning_sessions)||0,priority=attendance?(row.action_priority||row.severity||row.care_priority||'low'):(row.action_priority||row.severity||row.attention_level||'low');let reason='';if(attendance){if(learning<4)reason=`ARIA is learning ${row.first_name||'this person'}’s usual ${row.care_service_name||row.care_service_type||'gathering'} rhythm.`;else if(misses>=3)reason=`They usually attend this ${row.care_service_type||'gathering'} and have missed ${misses} in a row.`;else if(misses>=2)reason=`Their usual attendance pattern has changed for ${misses} consecutive sessions.`;else if(rate>=.75)reason=`They usually attend this ${row.care_service_type||'gathering'} (${Math.round(rate*100)}% of recent sessions).`;else reason='Their attendance pattern is still emerging.'}return{id:row.observation_id||row.action_id||`attendance:${row.person_id}:${row.care_session_id}`,person_id:row.person_id,first_name:row.first_name,last_name:row.last_name,phone:row.phone,priority,risk_level:priority,text:attendance?`${row.first_name||'This person'} was not recorded at ${row.care_session_name||row.care_service_type||'their usual gathering'}.`:row.observation_type?signalText({type:row.observation_type,evidence:row.evidence}):'ARIA has a pending action for this person.',observation_type:row.observation_type,observation_id:row.observation_id,confidence:row.confidence,severity:row.severity,urgency:row.urgency,attention_score:row.attention_score,evidence:row.evidence||null,detected_at:row.detected_at,action_id:row.action_id,action_type:row.action_type,action_status:row.action_status,suggestion:attendance?'Check in with this person':nextAction(row.observation_type,row.action_type?{type:row.action_type}:null),care_session_id:row.care_session_id,care_session_at:row.care_session_at,care_session_name:row.care_session_name,care_service_type:row.care_service_type,care_service_key:row.care_service_key,care_prior_attendance:Number(row.care_prior_attendance)||0,care_learning_sessions:learning,care_consecutive_misses:misses,care_attendance_rate:rate,care_last_contact_at:row.care_last_contact_at,care_reason:reason,engagement_state:row.engagement_state,care_state:row.care_state,relationship_state:row.relationship_state,followup_state:row.followup_state,attention_level:row.attention_level,open_observation_count:Number(row.open_observation_count)||0,open_action_count:Number(row.open_action_count)||0,state_updated_at:row.state_updated_at}}))}catch(err){console.error('[ARIA] Care Queue error:',err);return res.status(500).json({error:'Unable to load ARIA care queue.'})}}
async function report(req,res){const orgId=req.org.id,b=req.body||{},personId=String(b.person_id||''),kind=String(b.kind||'');if(!personId||!kind)return res.status(400).json({error:'person_id and kind are required'});if(!['travel','temporary_unavailable','preferred_service','not_attending','other'].includes(kind))return res.status(400).json({error:'Invalid care context'});try{const p=await pool.query(`SELECT id FROM people WHERE id=$1 AND organization_id=$2 AND status='active' LIMIT 1`,[personId,orgId]);if(!p.rows.length)return res.status(404).json({error:'Person not found'});let service=b.service_type||null;if(service){const s=String(service).trim().toLowerCase();const days={sunday:7,monday:1,tuesday:2,wednesday:3,thursday:4,friday:5,saturday:6};if(days[s])service=`weekday:${days[s]}`}const starts=b.starts_on||new Date().toISOString().slice(0,10),ends=b.ends_on||null,note=b.note||null;const r=await pool.query(`INSERT INTO aria_care_contexts(organization_id,person_id,kind,starts_on,ends_on,service_type,note,metadata,created_by)VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9)RETURNING id,kind,starts_on,ends_on,service_type,note`,[orgId,personId,kind,starts,ends,service,note,JSON.stringify(b.metadata||{}),req.user?.id||null]);if(note)await pool.query(`INSERT INTO timeline_events(people_id,event_type,title,description,metadata,source,occurred_at)VALUES($1,'care_context','ARIA care context',$2,$3,'care',$4)`,[personId,note,JSON.stringify({kind,ends_on:ends,service_type:service}),new Date().toISOString()]);return res.status(201).json(r.rows[0])}catch(e){console.error('[ARIA] Care report error:',e);return res.status(500).json({error:'Unable to save what you learned.'})}}
export default withOrg(handler);
