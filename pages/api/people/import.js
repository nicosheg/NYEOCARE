export default function handler(req,res){return res.status(410).json({error:'Bulk import is temporarily disabled until it is moved behind the canonical administrative data-import flow.',code:'LEGACY_BULK_IMPORT_ENDPOINT'});}
