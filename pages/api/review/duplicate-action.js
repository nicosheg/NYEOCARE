// pages/api/review/duplicate-action.js
import pool from'../../../lib/db';
import{withOrg}from'../../../lib/apiHelpers';
import{detectDuplicates}from'../../../lib/duplicateDetector';
async function person(db,id,org){const q=await db.query(`SELECT id,display_name,first_name,last_name,phone,phone_numbers,metadata,living_truth,identity_verification_status,identity_verified_at,identity_verified_by FROM people WHERE id=$1 AND organization_id=$2 AND status='active' FOR UPDATE`,[id,org]);return q.rows[0]||null}
function nameOf(p){return String(p.display_name||[p.first_name,p.last_name].filter(Boolean).join(' ')||p.first_name||'').trim()}
function phoneList(p){const v=Array.isArray(p.phone_numbers)?p.phone_numbers:p.phone?[p.phone]:[];return[...new Set(v.filter(Boolean).map(x=>typeof x==='string'?x:x?.normalized||x?.raw||x?.phone).filter(Boolean))]}
async function mergePeople(db,org,canonicalId,duplicateId,actorId,evidence){if(canonicalId===duplicateId)throw Object.assign(new Error('A person cannot be merged with themselves.'),{statusCode:400});const canonical=await person(db,canonicalId,org),duplicate=await person(db,duplicateId,org);if(!canonical||!duplicate)throw Object.assign(new Error('Both active people must still exist.'),{statusCode:404});await db.query(`SELECT id FROM people WHERE organization_id=$1 AND id=ANY($2::uuid[]) ORDER BY id FOR UPDATE`,[org,[String(canonicalId),String(duplicateId)].sort()]);const tables=['attendance_records:people_id','participation_records:person_id','timeline_events:people_id','person_aliases:person_id','person_memory:person_id','aria_learning:person_id','aria_brain_feed:person_id','aria_conversations:person_id','aria_care_contexts:person_id','aria_actions:person_id','aria_observations:person_id','aria_events:person_id','care_feedback:person_id','intelligence_outcomes:person_id','person_memberships:person_id','person_roles:person_id','person_field_values:person_id','person_lifecycle:person_id','person_documents:person_id','person_tasks:person_id','person_communications:person_id','person_segment_members:person_id','relationship_scores:person_id','engagement_metrics:person_id','people_intelligence:person_id'];for(const spec of tables){const[i,col]=spec.split(':');try{if(['relationship_scores','engagement_metrics','people_intelligence'].includes(i))await db.query(`DELETE FROM ${i} WHERE organization_id=$1 AND ${col}=$2 AND EXISTS(SELECT 1 FROM ${i} c WHERE c.organization_id=$1 AND c.${col}=$3)`,[org,duplicateId,canonicalId]);await db.query(`UPDATE ${i} SET ${col}=$1 WHERE ${col}=$2 AND EXISTS(SELECT 1 FROM people p WHERE p.id=$2 AND p.organization_id=$3)`,[canonicalId,duplicateId,org])}catch(err){throw Object.assign(new Error(`Could not safely move ${i} history: ${err.message}`),{statusCode:409})}}try{await db.query(`UPDATE person_relationships SET person_id=CASE WHEN person_id=$1 THEN $2 ELSE person_id END,related_person_id=CASE WHEN related_person_id=$1 THEN $2 ELSE related_person_id END WHERE organization_id=$3 AND (person_id=$1 OR related_person_id=$1)`,[duplicateId,canonicalId,org])}catch(err){throw Object.assign(new Error(`Could not safely move relationships: ${err.message}`),{statusCode:409})}try{await db.query(`DELETE FROM aria_person_state WHERE organization_id=$1 AND person_id=$2 AND EXISTS(SELECT 1 FROM aria_person_state x WHERE x.organization_id=$1 AND x.person_id=$3)`,[org,duplicateId,canonicalId]);await db.query(`UPDATE aria_person_state SET person_id=$1 WHERE organization_id=$2 AND person_id=$3`,[canonicalId,org,duplicateId])}catch(err){throw Object.assign(new Error(`Could not safely merge ARIA state: ${err.message}`),{statusCode:409})}try{await db.query(`INSERT INTO person_aliases(organization_id,person_id,alias,created_by) SELECT $1,$2,$3,$4 WHERE $3<>'' AND NOT EXISTS(SELECT 1 FROM person_aliases WHERE organization_id=$1 AND person_id=$2 AND lower(alias)=lower($3))`,[org,canonicalId,nameOf(duplicate),actorId])}catch(err){console.warn('[DUPLICATE] Alias preservation skipped:',err.message)}const mergedPhones=[...new Set([...phoneList(canonical),...phoneList(duplicate)])].slice(0,2),phoneJson=JSON.stringify(mergedPhones.map((p,i)=>({raw:p,normalized:p,source:'identity_merge',index:i+1})));const mergedMetadata={...(canonical.metadata||{}),identity_verified:true,last_identity_merge:{duplicate_id:duplicateId,merged_at:new Date().toISOString(),merged_by:actorId,evidence}},truth={...(canonical.living_truth||{}),status:'alive',source:'human_review',confirmed_at:(canonical.identity_verified_at||new Date().toISOString()),confirmed_by:(canonical.identity_verified_by||actorId)};await db.query(`UPDATE people SET phone=$2,phone_numbers=$3,identity_verification_status='verified',identity_verified_at=COALESCE(identity_verified_at,NOW()),identity_verified_by=COALESCE(identity_verified_by,$4),identity_verification_source=COALESCE(identity_verification_source,'human_review'),metadata=$5,living_truth=$6,updated_at=NOW() WHERE id=$1 AND organization_id=$7`,[canonicalId,mergedPhones[0]||null,phoneJson,actorId,mergedMetadata,truth,org]);await db.query(`UPDATE people SET status='archived',quarantine_reason=NULL,living_truth=jsonb_build_object('status','archived','source','identity_merge','merged_into',$2::text,'merged_at',NOW(),'merged_by',$3::text),metadata=metadata||jsonb_build_object('identity_merged_into',$2::text,'identity_merged_at',NOW(),'identity_merged_by',$3::text),updated_at=NOW() WHERE id=$1 AND organization_id=$4`,[duplicateId,canonicalId,actorId,org]);await db.query(`INSERT INTO identity_pair_decisions(organization_id,person_a_id,person_b_id,decision,decided_by,evidence,updated_at) VALUES($1,$2,$3,'duplicate_merged',$4,$5,NOW()) ON CONFLICT(organization_id,LEAST(person_a_id,person_b_id),GREATEST(person_a_id,person_b_id)) DO UPDATE SET decision='duplicate_merged',decided_by=EXCLUDED.decided_by,evidence=EXCLUDED.evidence,updated_at=NOW()`,[org,canonicalId,duplicateId,actorId,evidence]);return{canonical,duplicate}}
export default withOrg(async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const{action,canonicalId,duplicateId}=req.body||{};if(!['merge','keep_separate'].includes(action)||!canonicalId||!duplicateId)return res.status(400).json({error:'Invalid duplicate decision.'});const db=await pool.connect();try{await db.query('BEGIN');const ids=[String(canonicalId),String(duplicateId)].sort();await db.query(`SELECT id FROM people WHERE organization_id=$1 AND id=ANY($2::uuid[]) AND status='active' ORDER BY id FOR UPDATE`,[req.org.id,ids]);const audit=await detectDuplicates(req.org.id,{db});const pair=audit.duplicates.find(x=>[String(x.left.id),String(x.right.id)].includes(String(canonicalId))&&[String(x.left.id),String(x.right.id)].includes(String(duplicateId)));if(!pair){await db.query('ROLLBACK');return res.status(409).json({error:'This duplicate decision is no longer supported by current evidence.',code:'DUPLICATE_STALE'})}const evidence={score:pair.score,status:pair.status,evidence:pair.evidence,method:pair.method,detector_version:audit.detector_version};if(action==='keep_separate'){const a=await person(db,canonicalId,req.org.id),b=await person(db,duplicateId,req.org.id);const shared=[...new Set(phoneList(a).filter(x=>phoneList(b).includes(x)))];await db.query(`INSERT INTO identity_pair_decisions(organization_id,person_a_id,person_b_id,decision,decided_by,evidence,updated_at) VALUES($1,$2,$3,'keep_separate',$4,$5,NOW()) ON CONFLICT(organization_id,LEAST(person_a_id,person_b_id),GREATEST(person_a_id,person_b_id)) DO UPDATE SET decision='keep_separate',decided_by=EXCLUDED.decided_by,evidence=EXCLUDED.evidence,updated_at=NOW()`,[req.org.id,canonicalId,duplicateId,req.user.id,{...evidence,reason:'human_confirmed_separate'}]);for(const phone of shared)await db.query(`INSERT INTO identity_shared_contacts(organization_id,person_a_id,person_b_id,phone,confirmed_by,metadata) VALUES($1,$2,$3,$4,$5,$6) ON CONFLICT(organization_id,LEAST(person_a_id,person_b_id),GREATEST(person_a_id,person_b_id),phone) DO UPDATE SET confirmed_by=EXCLUDED.confirmed_by,confirmed_at=NOW(),metadata=EXCLUDED.metadata`,[req.org.id,canonicalId,duplicateId,phone,req.user.id,{reason:'human_confirmed_shared_contact'}]);await db.query(`UPDATE people SET identity_verification_status='verified',identity_verified_at=COALESCE(identity_verified_at,NOW()),identity_verified_by=COALESCE(identity_verified_by,$2),identity_verification_source=COALESCE(identity_verification_source,'human_review'),metadata=metadata||jsonb_build_object('identity_verified',true) WHERE id=ANY($1::uuid[]) AND organization_id=$3`,[ids,req.user.id,req.org.id]);await db.query('COMMIT');return res.status(200).json({ok:true,action:'kept_separate',shared_phones:shared.length})}await mergePeople(db,req.org.id,canonicalId,duplicateId,req.user.id,evidence);await db.query('COMMIT');return res.status(200).json({ok:true,action:'merged',canonical_id:canonicalId,archived_id:duplicateId,verified:true})}catch(err){try{await db.query('ROLLBACK')}catch{}console.error('[DUPLICATE] Action error:',err);return res.status(err.statusCode||500).json({error:err.statusCode?err.message:'Duplicate action could not be completed.',code:err.code})}finally{db.release()}});
